class CacheMovieInfo {
  final String
      id; //to save the file in this name so we can easily retrive the info

  CacheMovieInfo({required this.id});

  void saveMoviePoster() {
    String _path = " ";
  }
}
